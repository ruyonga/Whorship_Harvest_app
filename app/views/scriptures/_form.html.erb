<%= form_for(@scripture) do |f| %>
  <% if @scripture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scripture.errors.count, "error") %> prohibited this scripture from being saved:</h2>

      <ul>
      <% @scripture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :author %><br>
    <%= f.text_field :author %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
